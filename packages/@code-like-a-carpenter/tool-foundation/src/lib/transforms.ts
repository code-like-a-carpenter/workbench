// This file ensures the default transforms are installed automatically with the
// CLI so they're available as part of the default config.

import '@code-like-a-carpenter/foundation-transform-environment';
