{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "title": "Tool Tool",
  "description": "",
  "type": "object",
  "properties": {
    "buildBeforeRun": {
      "type": "boolean",
      "default": true,
      "description": "If true, this tool cannot be run without being built and, as such, some of the generated paths will be different. Note that _building_ the tool is left to an outside process."
    },
    "schemaDir": {
      "type": "string",
      "description": "A directory of json schemas defining each tool's inputs."
    }
  },
  "additionalProperties": false,
  "required": ["schemaDir"]
}
