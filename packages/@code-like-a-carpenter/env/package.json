{
  "name": "@code-like-a-carpenter/env",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild $(find ./src -name *.ts -o -name *.tsx) --outdir=dist --platform=node --sourcemap=external"
  }
}
